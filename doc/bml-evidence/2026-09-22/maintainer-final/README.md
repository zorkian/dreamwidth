# Maintainer form after heading correction

Replay after isolated server restart at `8986e7de8` passed real save/reload,
no JS/failed-resource responses, and disposable helper completion. Desktop and
390px narrow captures retain the selected override, reason and checkbox. The
HTTP regression independently asserts exactly one rendered page heading.
Runner log: `/tmp/bml-maintainer-final.log` in foreman container.
