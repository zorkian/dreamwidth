# Native S2 language initialization replay

Captured after isolated server restart at `d50c11eea`, including `ea61aa63d`.
All six ordinary views assert HTTP200, no JS errors and no failed resources.
Disposable helper exits successfully. First500 captured body characters match
controlstrip-native after normalizing disposable usernames; this is bounded
text evidence, not pixel equivalence. Full-page screenshots and results.json
are retained. Runner/log: `/tmp/bml-s2-language-native.js` and `.log`.
