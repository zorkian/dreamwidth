# Modern entry draft and preview

Foreman replay at `f86d593dd`, after clean server restart, passed actual
HTML/RTE draft save, restore, clear and visible preview assertions. The
disposable fixture verified unchanged fresh entry count and completed cleanup.
Exactly one expected restoration dialog occurred. Captures show restored RTE
and rendered preview; log: host `/tmp/bml-entry-draft-integrated.log`.

This is modern-flow characterization, not legacy endpoint retirement approval.

Foreman visual inspection confirms the restored title and formatted RTE body,
and the preview title/body plus explicit preview-only notice.
