# Native settings hub acceptance

Captured in foreman container `8d7783a043d8` at integrated `035e27196`.
Fifteen matching category/actor states use disposable users and a managed
community; helper completion is awaited. All return200 with no JS or failed
resources. `results.json` records forms and runtime observations. Extra
anonymous/authenticated narrow screenshots come from the full mutation suite.

Commands: `node /tmp/bml-settings-after.js` (capture script and disposable helper
retained in host/container /tmp), and `node t/browser/settings.js`.
Logs: `/tmp/bml-settings-integrated-captures.log` and
`/tmp/bml-settings-integrated-browser.log`.

Compare ../settings-before. The former Other Sites JavaScript exception is gone.
Both default local notification screenshots show No notification methods because
the dev Email configuration is unavailable; configured subscription controls and
persistence are exercised by real HTTP tests with an explicit Inbox method.
No seeded account settings were modified for these after captures.
