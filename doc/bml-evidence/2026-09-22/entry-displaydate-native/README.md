# Visible Display Date controls

Foreman replay after integration `8823c34c1` passes17 timestamp plus17 rendering
assertions, then the disposable browser checks at1280px and375px. The fixture
enables the panel through its existing per-user setting. Each input is scrolled
into view and has a nonzero viewport intersection plus its translated label.
Foreman visual inspection of narrow.png confirms the panel and date/time
controls are visible and fit. Browser and helper complete successfully.

Log: host `/tmp/bml-entry-displaydate-browser.log`; HTTP log inside container
`/tmp/bml-entry-displaydate-integrated.log`.
