# Legacy community maintainer editor baseline

Captured at foreman `03633bbac` using disposable manager, other poster and
community accounts. The helper stays alive until the browser closes, then its
completion is awaited. No editor action is submitted. Local probe scripts are
`/tmp/bml-editor-manager-browser.js` and `/tmp/bml-editor-manager-fixture.pl`;
container log is `/tmp/bml-editor-manager-browser.log` in `8d7783a043d8`.

- legacy-desktop.png: disabled subject/body; translated administrator age,
  reason and comment overrides; save override, delete, and spam-delete actions.
- legacy-narrow.png: existing overflow, measured body width648 at viewport390.
- modern-gap.png: same actor and entry receive the existing IS AN ADMIN error.

This is baseline characterization, not a clean browser acceptance claim.
`state.json` records two existing XPostAccount.xpostAcctUpdated null.checked
exceptions, one per legacy render, and no failed network resources. The future
modern maintainer implementation must avoid this exception and narrow overflow.
The separate HTTP probe confirms forced-fresh entry body preservation.
