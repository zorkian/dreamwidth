# Callable community edit GET evidence

Captured in the foreman isolated devcontainer at98c4e1107 using the accepted
callable-only browser server. Public community GET activation is separate.

- `community-same-poster-{1280,390}.png`: native ordinary editor, members-only
  entry, canonical action and usable subject/body controls.
- `community-manager-{1280,390}.png`: native property-only manager form with
  selected age override, reason and checked comment-disable control.

Foreman normal browser PASS, fresh entry/user draft/preferences preserved and
fixture/server process checks empty. Log `/tmp/bml-community-callable-browser.log`.
Both390px captures visually inspected: forms fit, controls visible, single heading.
Independent Sol reviewed6860/c4 normal, named failure cleanup and all captures.

The property-only manager renderer does not replace the retained manager page's
full action surface. Public manager GET remains BML until delete and delete-as-spam
controls have a separately reviewed replacement.
