# Public owned-edit dispatch browser evidence

Foreman replay at `06735ba3a` through real `app.psgi` routing, with no test route
overlay. Disposable fixture, isolated server and exact generated assets.

The native invalid-date correction form retains submitted content and private
security, shows one visible validation error, and fits desktop and 390px widths.
The same run then corrects the date, saves distinct retry subject/body, verifies
force-fresh persistence and deletes only the selected entry. Retained GET baseline
captures are in the adjacent `legacy-owned-edit-adapter` folder.

Normal run passed in `/tmp/bml-owned-edit-public-integrated.log`, container
`8d7783a043d8`; no fixture/server remained. Sol independently passed the normal
and named intentional-failure flows at source `239e355bc`. This validates only
the accepted personal-owner save/delete subset; unsupported BML paths remain.
