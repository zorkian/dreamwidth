# Native preview integration

Captured with the disposable real popup browser test at local `abc509db6`.
The legacy POST schema now uses the shared native renderer. Both old and
modern popup controls pass route/content/warning, action/target restoration,
password control restoration and fresh entry-count nonmutation assertions.

Foreman browser exit0, no JavaScript/HTTP failures, helper absent after exit.
Host log `/tmp/bml-entry-preview-native.log`. Before captures are in adjacent
`entry-preview-before/`. Legacy BML file is retained pending GET/HEAD retirement
compatibility audit; these captures do not claim complete editor retirement.
