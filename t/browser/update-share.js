// Browser acceptance for callable authenticated update share GET rendering.
// Copyright (c) 2026 by Dreamwidth Studios, LLC. Same terms as Perl itself.
const assert = require('node:assert/strict');
const fs = require('node:fs');
const net = require('node:net');
const {spawn} = require('node:child_process');
const puppeteer = require('/opt/dw-screenshot/node_modules/puppeteer-core');
const port = Number(process.env.UPDATE_SHARE_PORT || 18143);
const output = process.argv[2] || '/tmp/update-share-browser';
function unusedPort() { return new Promise((resolve,reject)=>{const s=net.connect(port,'127.0.0.1');s.once('connect',()=>{s.destroy();reject(Error(`share port ${port} occupied`));});s.once('error',e=>{s.destroy();e.code==='ECONNREFUSED'?resolve():reject(e);});}); }
function waitPort() { return new Promise((resolve,reject)=>{const end=Date.now()+15000;const go=()=>{const s=net.connect(port,'127.0.0.1');s.once('connect',()=>{s.destroy();resolve();});s.once('error',()=>{s.destroy();Date.now()>end?reject(Error('share server did not listen')):setTimeout(go,100);});};go();}); }
function childDone(child,label) { return new Promise((resolve,reject)=>{child.once('error',reject);child.once('exit',(code,signal)=>code===0&&!signal?resolve({code,signal}):reject(Error(`${label} EOF ${code}/${signal||'none'}`)));}); }
function lines(child,done) { let buffer='';const queue=[];const waiters=[];let stopped; const drain=()=>{while(queue.length&&waiters.length)waiters.shift().resolve(queue.shift());};child.stdout.on('data',chunk=>{buffer+=chunk;let n;while((n=buffer.indexOf('\n'))>=0){queue.push(buffer.slice(0,n));buffer=buffer.slice(n+1);}drain();});done.catch(e=>{stopped=e;while(waiters.length)waiters.shift().reject(e);});return()=>new Promise((resolve,reject)=>{if(stopped)return reject(stopped);if(queue.length)return resolve(queue.shift());waiters.push({resolve,reject});}); }
async function stop(child,done,label){if(!child)return;if(child.exitCode===null)child.kill('SIGTERM');try{await done;}catch(e){if(!/SIGTERM/.test(e.message))throw e;}}
(async()=>{let fixture,server,browser,fixtureDone,serverDone;const errors=[],failures=[];try{await unusedPort();fixture=spawn('perl',[process.env.LJHOME+'/t/browser/update-get-fixture.pl'],{stdio:['pipe','pipe','inherit']});fixtureDone=childDone(fixture,'fixture');const next=lines(fixture,fixtureDone);const startup=JSON.parse(await Promise.race([next(),fixtureDone.then(()=>Promise.reject(Error('fixture exited before startup')))]));server=spawn('perl',[process.env.LJHOME+'/t/browser/update-share-server.pl',String(port)],{stdio:['ignore','ignore','inherit']});serverDone=childDone(server,'server');await Promise.race([waitPort(),serverDone.then(()=>Promise.reject(Error('server exited before listening')))]);browser=await puppeteer.launch({executablePath:'/usr/bin/google-chrome-stable',args:['--no-sandbox']});const page=await browser.newPage();page.on('pageerror',e=>errors.push(e.message));page.on('requestfailed',r=>failures.push(r.url()));page.on('response',r=>{if(r.status()>=400)failures.push(`${r.status()}:${r.url()}`);});fs.mkdirSync(output,{recursive:true});await page.goto(`http://127.0.0.1:${port}/mobile/login`,{waitUntil:'networkidle0'});await page.type('[name=user]',startup.user);await page.type('[name=password]',startup.password);await Promise.all([page.waitForNavigation({waitUntil:'networkidle0'}),page.click('[type=submit]')]);fixture.stdin.write(JSON.stringify({state:1})+'\n');const before=JSON.parse(await Promise.race([next(),fixtureDone]));for(const width of [1280,390]){await page.setViewport({width,height:844});await page.goto(`http://127.0.0.1:${port}/__test_update_share?share=https%3A%2F%2Fexample.invalid%2Finput&prop_taglist=browser-tag&encoded=a%2Fb&repeated=one&repeated=two`,{waitUntil:'networkidle0'});await page.waitForSelector('#js-post-entry');assert.equal(await page.title(),'Post an Entry');assert.equal(await page.$eval('[name=subject]',e=>e.value),'Browser shared title');assert.match(await page.$eval('[name=event]',e=>e.value),/https:\/\/example\.invalid\/browser-share/);assert.equal(await page.$eval('[name=taglist]',e=>e.value),'browser-tag');assert.equal(await page.$eval('[name=editor]',e=>e.value),'rte0');assert.match(await page.$eval('#js-post-entry',e=>e.action),/\/entry\/new\?share=/);const usable=await page.$eval('[name=subject]',e=>{const r=e.getBoundingClientRect();return r.width>0&&r.left>=0&&r.right<=innerWidth;});assert.ok(usable,`subject usable ${width}`);await page.screenshot({path:`${output}/share-${width}.png`,fullPage:true});}fixture.stdin.write(JSON.stringify({state:1})+'\n');const after=JSON.parse(await Promise.race([next(),fixtureDone]));assert.deepEqual(after,before,'share GET leaves draft/editor state unchanged');if(process.env.UPDATE_SHARE_INTENTIONAL_FAIL)throw Error('intentional share cleanup failure');assert.deepEqual(errors,[]);assert.deepEqual(failures,[]);console.log('PASS update share browser');}finally{try{if(browser)await browser.close();}finally{try{await stop(server,serverDone,'server');}finally{if(fixture){if(fixture.exitCode===null&&!fixture.stdin.destroyed)fixture.stdin.end();await fixtureDone;}}}}})().catch(e=>{console.error(e);process.exit(1)});
